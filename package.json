{
  "name": "kynetrapdf",
  "private": true,
  "version": "1.0.0",
  "description": "KynetraPDF production-ready PDF platform foundation",
  "scripts": {
    "dev:frontend": "python -m http.server 4173 --directory frontend",
    "dev:backend": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
